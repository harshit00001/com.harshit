package com.harshit.parkinglot.enums;

/**
 * ENUM - types of parking spots.
 *
 * Compact spots are smaller. Large spots can fit bigger vehicles.
 * Keeping this as enum makes spot rules easy to read in code.
 */
public enum SpotType {
    COMPACT,
    LARGE
}
