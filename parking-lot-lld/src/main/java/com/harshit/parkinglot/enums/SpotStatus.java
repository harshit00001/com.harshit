package com.harshit.parkinglot.enums;

/**
 * ENUM - current state of a parking spot.
 *
 * Only two states are needed in this basic design:
 * AVAILABLE (empty) or OCCUPIED (vehicle parked).
 */
public enum SpotStatus {
    AVAILABLE,
    OCCUPIED
}
